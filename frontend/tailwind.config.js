// Tailwind CSS v4 configuration is handled entirely via CSS in src/index.css.
// The @theme {} block in index.css defines all design tokens (colors, fonts, breakpoints).
// A separate tailwind.config.js is NOT required when using @tailwindcss/vite (v4 plugin).
